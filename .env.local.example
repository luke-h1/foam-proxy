# Copy to .env.local (gitignored) and fill in. Loaded by `make run-local`.

# proxy
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
# Local fallback for /api/magic. Leave MAGIC_LINK_SSM_PARAM unset so it doesn't hit SSM.
MAGIC_LINK_BLOB=
MAGIC_LINK_API_KEY=

# authorizer
API_KEY=
